# Base URL of the backend API. Copy to .env to override.
#
# Local dev (default):
VITE_API_URL=http://localhost:8000/api
#
# Production (set this in the Vercel dashboard, pointing at the Render backend):
#   VITE_API_URL=https://issuehub-backend.onrender.com/api
